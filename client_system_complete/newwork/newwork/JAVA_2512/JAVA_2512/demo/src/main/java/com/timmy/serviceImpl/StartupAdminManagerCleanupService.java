package com.timmy.serviceImpl;

import org.springframework.stereotype.Service;

@Service
public class StartupAdminManagerCleanupService {
	// Automatic startup cleanadmin behavior is intentionally disabled so
	// device-side admin/manager roles are preserved after the EXE starts.
}
