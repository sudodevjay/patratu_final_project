<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.timmy.mapper.ClimsViewMapper" >
  <resultMap id="BaseResultMap" type="com.timmy.entity.ClimsViewRecord" >
    <id column="device_log_id" property="deviceLogId" jdbcType="BIGINT" />
    <result column="download_date" property="downloadDate" jdbcType="VARCHAR" />
    <result column="project_id" property="projectId" jdbcType="VARCHAR" />
    <result column="user_id" property="userId" jdbcType="BIGINT" />
    <result column="log_date" property="logDate" jdbcType="VARCHAR" />
    <result column="direction" property="direction" jdbcType="VARCHAR" />
    <result column="device_id" property="deviceId" jdbcType="BIGINT" />
  </resultMap>

  <sql id="Base_Column_List" >
    CONVERT(BIGINT, DEVICELOGID) AS device_log_id,
    CONVERT(VARCHAR(19), DOWNLOADDATE, 120) AS download_date,
    PROJECTID AS project_id,
    CASE WHEN USERID IS NULL THEN NULL ELSE CONVERT(BIGINT, USERID) END AS user_id,
    CONVERT(VARCHAR(19), LOGDATE, 120) AS log_date,
    DIRECTION AS direction,
    CASE WHEN DEVICEID IS NULL THEN NULL ELSE CONVERT(BIGINT, DEVICEID) END AS device_id
  </sql>

  <select id="selectAll" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM CLIMSVIEW
    ORDER BY DEVICELOGID DESC
  </select>
</mapper>
